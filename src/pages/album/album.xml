<scroll-view class="list" scroll-y="{{true}}" enable-back-to-top="{{true}}">
    <navigator class="pic-wrapper" url="/pages/picture/picture?id={{item.id}}" wx:for="{{picSrcs}}" wx:key="{{item}}">
        <image mode="aspectFill" src="{{item.src}}"/>
    </navigator>
</scroll-view>